#pragma once

//PCH HEADER

//#include <algorithm>		<- Doesn't compile
//#include <functional>		<- Doesn't compile
//#include <iostream>		<- Doesn't compile
//#include <string>			<- Doesn't compile
//#include <sstream>		<- Doesn't compile

//#include <vector>			<- Doesn't compile
//#include <list>			<- Doesn't compile
//#include <map>			<- Doesn't compile
//#include <unordered_map>	<- Doesn't compile

#include <cstddef>
#include <stdio.h>
#include <math.h>

#include "Common.h"
#include "Types.h"